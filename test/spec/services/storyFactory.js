'use strict';

describe('Service: Storyfactory', function () {

  // load the service's module
  beforeEach(module('wolofApp'));

  // instantiate service
  var Storyfactory;
  beforeEach(inject(function (_Storyfactory_) {
    Storyfactory = _Storyfactory_;
  }));

  //it('should do something', function () {
  //  expect(!!Storyfactory).toBe(true);
  //});

});
